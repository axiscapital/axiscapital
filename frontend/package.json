
{
  "name": "axiscapital-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.6.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.0"
  },
  "scripts": {
    "start": "npx serve -s build",
    "build": "npx babel src --out-dir build --copy-files"
  }
}
